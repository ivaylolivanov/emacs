#ifndef MAP
#define MAP

#include"Game.hpp"

class Map
{
private:
    SDL_Rect src, dest;
    SDL_Texture* ground;
    SDL_Texture* coast;
    SDL_Texture* lava;

    int map[20][25];

public:
    Map();
    ~Map();

    void loadMap( int arr[20][25] );
    void drawMap();

};

#endif
