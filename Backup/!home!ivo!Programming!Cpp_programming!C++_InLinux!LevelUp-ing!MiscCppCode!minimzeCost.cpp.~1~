#include<bits/stdc++.h>
using namespace std;

long long Solve (long long k, vector<int> arr) {
   // Write code here
}

int main() {

    ios::sync_with_stdio(0);
    cin.tie(0);
    long long k;
       int n;
    cin >> n;
    cin >> k;

    vector<int> arr(n);
    for(int i_arr=0; i_arr<n; i_arr++)
    {
    	cin >> arr[i_arr];
    }

    long long out_;
    out_ = Solve(k, arr);
    cout << out_;
}
