#include <stdio.h>
#include <iostream>
#include <vector>
#include <algorithm>
#include <iomanip>
using namespace std;

int main(){

    int i ,j, n, m;
    double a[MAXN];
    double b[MAXN];
    vector<double> result;

    cin >> m >> n;
    for(i=0;i<m;i++){
	cin >> a[i];
	double rounded = a[i] = (int)(a[i] * 100.0)/100.0;
	a[i] = rounded;
    }

    for(j=0;j<n;j++){
	cin >> b[j];
	double rounded2 = b[j] = (int)(b[j] * 100.0)/100.0;
	b[j] = rounded2;
    }


    for(int first_array = 0; first_array < m; first_array++){
	bool equal_ = false;
	for(int second_array = 0; second_array < n; second_array++){
	    if(a[first_array] == b[second_array]){
		equal_ = true;
	    }
	}
	if(equal_ == false){
	    result.push_back(a[first_array]);
	}
    }
    for(int s_array = 0; s_array < n; s_array++){
	bool equal_ = false;
	for(int f_array = 0; f_array < m; f_array++){
	    if(b[s_array] == a[f_array]){
		equal_ = true;
	    }
	}
	if(equal_ == false){
	    result.push_back(b[s_array]);
	}
    }
    sort(result.begin(), result.end());
    int size_ = result.size();
    for (int g = 0; g < size_ ; g++){
	cout << result[g] << " ";
    }

    return 0;

}
